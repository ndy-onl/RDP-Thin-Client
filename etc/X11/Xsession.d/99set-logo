#!/bin/sh
LOGO_PATH="/usr/local/share/branding/logo-login.png"

# Ensure feh is installed
if ! command -v feh &>/dev/null; then
    apt-get update && apt-get install -y feh
fi

# Position logo in the bottom-right corner with offset
feh --bg-center --no-xinerama --geometry x120-40-40 "$LOGO_PATH"
