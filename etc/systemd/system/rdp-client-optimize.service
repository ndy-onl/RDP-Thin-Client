[Unit]
Description=Optimize system for RDP thin client
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c "systemctl stop cups bluetooth avahi-daemon; systemctl mask cups bluetooth avahi-daemon"
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
